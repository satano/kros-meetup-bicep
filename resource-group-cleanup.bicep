// Empty Bicep template for deleting all resources in resource group.
// Just run Azure CLI deployment in 'Complete' mode.
//
// az deployment group create --resource-group {resource-group-name} --template-file resource-group-cleanup.bicep --mode Complete
